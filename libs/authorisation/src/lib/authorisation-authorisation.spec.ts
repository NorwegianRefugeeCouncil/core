import { authorisationAuthorisation } from './authorisation-authorisation';

describe('authorisationAuthorisation', () => {
  it('should work', () => {
    expect(authorisationAuthorisation()).toEqual('authorisation-authorisation');
  });
});
